# Deterministic-only mode is the default and requires no API keys.
LLM_ENABLED=false
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Backend text-to-speech is optional. Keep disabled unless you have an OpenAI key.
TTS_ENABLED=false
OPENAI_API_KEY=
TTS_VOICE=alloy
TTS_MODEL=tts-1
